# Authors
Aneesh Iyer
Logan Stapleton
Daniyaal Rasheed
Liam E Roach

# to get the cli up-and-running:
`./run install` installs dependencies as long as you have the ability to execute go
`./run compile` complies the cli
`./run URL_FILE` will run the cli with an ascii delimited file containing ULRs to modules
